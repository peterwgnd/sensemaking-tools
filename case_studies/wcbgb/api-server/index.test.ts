// TODO: At least one test is required for each repository in the monorepo, but tests haven't been
// written yet. This test is a placeholder and should be replaced.

describe("Server Tests", () => {
  it("Test #1", async () => {
    expect(1 + 1).toEqual(2);
  });
});
